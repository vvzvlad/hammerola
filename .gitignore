.env
.venv/
venv/
ENV/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# Test-run artefacts. Regenerated by `make test` and by pytest-cov; committing them makes
# every diff carry a binary blob whose content depends on which tests happened to run.
.pytest_cache/
.coverage
htmlcov/
*.swp
.DS_Store
logs/
data/*
!data/.gitkeep
# Claude Code agent worktrees — never commit
.claude/worktrees/
